version: 1

on:
  push:
    deployments:
      - target: dev
        cwd: /opt/galera
    commands:
      - git pull --ff-only
      - npm ci
      - npm run check
      - npm test
      - npm run lint
