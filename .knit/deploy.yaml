version: 1

on:
  push:
    deployments:
      - target: dev
        cwd: /opt/elera
        commands:
          - git pull --ff-only
          - npm ci
          - npm run check
          - npm test
          - npm run lint
          - npm run audit
          - npm run pack
