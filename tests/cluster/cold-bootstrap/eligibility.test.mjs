import { evaluateColdBootstrap } from '../../../src/cluster/cold-bootstrap/eligibility.mjs';
const nodes = [{ name: 'a', dataDir: '/a' }, { name: 'b', dataDir: '/b' }];
test('requires every node to be offline and evaluates recovered seqnos', async () => { const result = await evaluateColdBootstrap(nodes, { isOnline: async () => false, readState: async (node) => ({ uuid: 'cluster', seqno: node.name === 'a' ? -1 : 8, safeToBootstrap: false }), recover: async () => ({ uuid: 'cluster', seqno: 9, recovered: true }) }); expect(result).toMatchObject({ eligible: true, candidate: { node: 'a', seqno: 9 } }); });
test('refuses an online member before selecting a candidate', async () => { await expect(evaluateColdBootstrap(nodes, { isOnline: async (node) => node.name === 'b', readState: async () => ({ uuid: 'cluster', seqno: 1, safeToBootstrap: false }) })).resolves.toMatchObject({ eligible: false, reason: 'node is still online: b' }); });
test('requires nodes and state readers', async () => { await expect(evaluateColdBootstrap([])).rejects.toThrow('nodes'); await expect(evaluateColdBootstrap(nodes)).rejects.toThrow(); });
test('fails closed when a peer disappears during evidence collection', async () => { const result = await evaluateColdBootstrap(nodes, { isOnline: async () => false, readState: async () => { throw new Error('peer unavailable'); } }); expect(result).toMatchObject({ eligible: false, reason: 'node evidence unavailable: a' }); });
test('uses the default offline check while recovering a negative seqno', async () => { const result = await evaluateColdBootstrap(nodes, { readState: async (node) => ({ uuid: 'cluster', seqno: node.name === 'a' ? -1 : 2, safeToBootstrap: false }), recover: async () => ({ uuid: 'cluster', seqno: 3 }) }); expect(result.eligible).toBe(true); });
