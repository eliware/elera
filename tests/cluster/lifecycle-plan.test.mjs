import { planLifecycle } from '../src/cluster/lifecycle-plan.mjs';
test('plans bootstrap and join eligibility', () => { expect(planLifecycle('bootstrap', { enabled: true, ready: false, quorum: false }).eligible).toBe(true); expect(planLifecycle('join', { enabled: true, ready: false, quorum: true, nodeId: 'n', target: 'p' }).eligible).toBe(true); });
test('rejects unsafe lifecycle actions', () => { expect(() => planLifecycle('bad', {})).toThrow(); expect(planLifecycle('bootstrap', { enabled: false }).eligible).toBe(false); expect(planLifecycle('join', { enabled: true, ready: false, quorum: false }).reason).toContain('identity'); expect(planLifecycle('leave', { enabled: true, ready: true, synced: false, quorum: true, nodeId: 'n', target: 'p' }).eligible).toBe(false); expect(planLifecycle('recover', { enabled: true, ready: false, quorum: false, nodeId: 'n', target: 'p' }).eligible).toBe(false); });
test('covers lifecycle rejection reasons and eligible leave/recovery', () => { expect(planLifecycle('bootstrap', { enabled: true, ready: true }).reason).toContain('already'); expect(planLifecycle('bootstrap', { enabled: true, ready: false, quorum: true }).reason).toContain('quorum'); expect(planLifecycle('join', { enabled: true, ready: true, quorum: true, nodeId: 'n', target: 'p' }).reason).toContain('already'); expect(planLifecycle('join', { enabled: true, ready: false, quorum: false, nodeId: 'n', target: 'p' }).reason).toContain('established'); expect(planLifecycle('leave', { enabled: true, ready: false, synced: true, quorum: true, nodeId: 'n', target: 'p' }).reason).toContain('ready'); expect(planLifecycle('leave', { enabled: true, ready: true, synced: true, quorum: false, nodeId: 'n', target: 'p' }).reason).toContain('quorum'); expect(planLifecycle('leave', { enabled: true, ready: true, synced: true, quorum: true, nodeId: 'n', target: 'p' }).eligible).toBe(true); expect(planLifecycle('recover', { enabled: true, ready: true, quorum: true, nodeId: 'n', target: 'p' }).reason).toContain('offline'); expect(planLifecycle('recover', { enabled: true, ready: false, quorum: true, nodeId: 'n', target: 'p' }).eligible).toBe(true); });
