import { createMetadataService } from '../src/metadata/service.mjs';
test('initializes metadata and verifies the schema', async () => { const calls = []; const query = async (sql) => { calls.push(sql); if (sql.startsWith('SELECT version')) return [[{ version: 1 }]]; return [[]]; }; const service = createMetadataService({ query }); const result = await service.initialize(); expect(result.initialized).toBe(true); expect(result.database).toBe('elera_meta'); expect(calls.some(sql => sql.includes('CREATE DATABASE'))).toBe(true); expect((await service.verify()).verified).toBe(true); });
test('reports uninitialized metadata and validates dependencies', async () => { expect(() => createMetadataService({})).toThrow('query function'); const service = createMetadataService({ query: async () => { throw new Error('missing'); } }); expect((await service.status()).initialized).toBe(false); });
