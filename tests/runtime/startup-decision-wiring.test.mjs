import { expect, jest, test } from '@jest/globals';
import { resolveExplicitSupervisorStartup } from '../../src/runtime/startup-decision-wiring.mjs';
test('returns unchanged args without explicit decision', async () => { const args = ['--x']; await expect(resolveExplicitSupervisorStartup({ environment: {}, nodeName: 'node', args, decision: () => undefined })).resolves.toEqual({ explicit: false, decision: undefined, args }); });
test('preserves explicit non-bootstrap decisions', async () => { const decision = { mode: 'join', reason: 'operator' }; await expect(resolveExplicitSupervisorStartup({ environment: {}, nodeName: 'node', args: [], decision: () => decision })).resolves.toEqual({ explicit: true, decision, args: [] }); });
test('promotes and adds bootstrap arguments', async () => { const promote = jest.fn(); const addArguments = jest.fn(() => ['--bootstrap']); const decision = { mode: 'bootstrap', epoch: 2 }; await expect(resolveExplicitSupervisorStartup({ environment: {}, nodeName: 'node', dataDir: '/data', args: [], decision: () => decision, promote, addArguments })).resolves.toEqual({ explicit: true, decision, args: ['--bootstrap'] }); expect(promote).toHaveBeenCalledWith('/data/grastate.dat'); });
